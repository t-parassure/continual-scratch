# Scratch Demo Shop

A deliberately imperfect demo storefront used by Continual to demonstrate its
safety gates end to end (fix PR → merge → post-merge watch → auto-revert →
circuit breaker) on a real merge→deploy cycle, without touching any customer app.
Hosted on GitHub Pages; merge to main publishes.
